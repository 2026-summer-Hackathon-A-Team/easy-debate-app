services:
  # backendコンテナ
  backend:
    # backendコンテナのproductionステージを指定してビルドする
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: production 
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy

volumes:
  mysql_data: